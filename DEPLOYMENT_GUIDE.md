# دليل النشر الشامل للمشروع

## 🎯 الخطوات الأساسية

### المرحلة 1: الإعداد الأولي (مرة واحدة فقط)

#### أ) ربط Vercel بـ GitHub
1. اذهب إلى https://vercel.com/new
2. انقر على "Continue with GitHub"
3. أعطِ Vercel صلاحيات الوصول
4. حدد مستودع `educational-dashboard`
5. انقر على "Import"

#### ب) تعيين متغيرات البيئة
1. في لوحة Vercel، انقر على "Settings"
2. اختر "Environment Variables"
3. أضف هذه المتغيرات:

| المتغير | القيمة | الملاحظة |
|---------|--------|---------|
| NEXT_PUBLIC_SUPABASE_URL | من Supabase | الرابط العام |
| NEXT_PUBLIC_SUPABASE_ANON_KEY | من Supabase | المفتاح العام |

4. تأكد من أن جميع التطبيقات (Production, Preview, Development) تستخدم نفس المتغيرات

---

### المرحلة 2: النشر المحلي (يومياً)

#### الخطوة 1: تحديث المشروع
\`\`\`bash
# سحب أحدث التحديثات
git pull origin main

# تثبيت المكتبات الجديدة (إن وجدت)
npm install
\`\`\`

#### الخطوة 2: اختبار محلي
\`\`\`bash
# بناء المشروع
npm run build

# اختبار التشغيل
npm run dev

# الدخول إلى http://localhost:3000
\`\`\`

#### الخطوة 3: إضافة التغييرات
\`\`\`bash
# عرض التغييرات
git status

# إضافة جميع التغييرات
git add .

# عرض ما سيتم إرساله
git diff --cached

# إنشاء Commit بوصف واضح
git commit -m "feat: إضافة ميزة جديدة - [اسم الميزة]"
\`\`\`

#### الخطوة 4: الدفع إلى المستودع
\`\`\`bash
# دفع التغييرات
git push origin main

# انتظر قليلاً لرؤية حالة النشر
\`\`\`

---

### المرحلة 3: المراقبة والتحقق

#### تتبع حالة النشر
1. اذهب إلى https://vercel.com/dashboard
2. حدد المشروع `educational-dashboard`
3. انقر على "Deployments"
4. شاهد حالة النشر الحالية

#### الحالات الممكنة:
- 🟢 **Building** - جاري البناء
- 🔵 **Ready** - تم النشر بنجاح
- 🔴 **Error** - حدث خطأ

#### حل المشاكل الشائعة

**1. فشل البناء (Build Error)**
\`\`\`
الحل:
1. اذهب إلى Vercel > Deployments > [آخر نشر]
2. انقر على "Build Logs"
3. ابحث عن رسالة الخطأ الحمراء
4. اصلح المشكلة محلياً
5. أعد النشر
\`\`\`

**2. مشكلة في المتغيرات (Environment Variables)**
\`\`\`bash
# تحقق من البيئة المحلية
cat .env.local

# تأكد من وجود جميع المتغيرات على Vercel
\`\`\`

**3. فشل اتصال قاعدة البيانات**
\`\`\`
الحل:
1. تحقق من أن Supabase مفعل ويعمل
2. تحقق من صحة المفاتيح على Vercel
3. اختبر الاتصال محلياً
\`\`\`

---

## 📋 الملفات المهمة للنشر

### أثناء النشر، تأكد من:

✅ **package.json** - جميع المكتبات مدرجة
✅ **next.config.mjs** - الإعدادات صحيحة
✅ **tsconfig.json** - التكوين صحيح
✅ **.env.local** - المتغيرات موجودة محلياً
✅ **vercel.json** - (اختياري) لتخصيص الإعدادات

### لا تنسَ:
❌ **لا تشارك** `.env.local` مع أحد
❌ **لا تضع** كلمات المرور في الـ Git
❌ **لا تنسَ** تعيين المتغيرات على Vercel

---

## 🚀 أفضل الممارسات

### 1. Commit Messages
\`\`\`bash
# صيغة جيدة
git commit -m "feat: إضافة زر تعديل البيانات"
git commit -m "fix: إصلاح خطأ في قاعدة البيانات"
git commit -m "docs: تحديث التوثيق"

# صيغة سيئة
git commit -m "update"
git commit -m "test"
\`\`\`

### 2. اختبار قبل النشر
\`\`\`bash
# تشغيل محلي كامل
npm run build && npm run dev

# التحقق من عدم وجود أخطاء TypeScript
npm run type-check
\`\`\`

### 3. البقاء محدثاً
\`\`\`bash
# اسحب التحديثات قبل البدء
git pull origin main

# تحقق من فرع العمل الحالي
git branch
\`\`\`

---

## 📊 الأوامر السريعة

\`\`\`bash
# عرض آخر النشرات
git log --oneline -10

# تراجع عن آخر Commit
git reset --soft HEAD~1

# عرض الفرق بين المحلي والمستودع
git diff origin/main

# نسخ Commit من فرع آخر
git cherry-pick [commit-hash]
\`\`\`

---

## ✅ قائمة التحقق قبل النشر

- [ ] اختبرت المشروع محلياً
- [ ] لا توجد رسائل خطأ في Console
- [ ] التصميم يبدو صحيح
- [ ] جميع الأزرار تعمل
- [ ] قاعدة البيانات تستجيب
- [ ] رسائل النجاح تظهر بشكل صحيح
- [ ] كتبت Commit message واضح
- [ ] تأكدت من البيانات المهمة
- [ ] راجعت التغييرات قبل الإرسال

---

## 📞 للمساعدة

إذا واجهت مشاكل:
1. اقرأ logs على Vercel
2. تحقق من Environment Variables
3. اختبر محلياً أولاً
4. ابحث عن الخطأ في الـ docs
5. ابدأ من جديد إذا لزم الأمر
