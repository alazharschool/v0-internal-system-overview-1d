# 📋 دليل إصلاح قاعدة البيانات - Al-Azhar School System

## 🔴 المشاكل المكتشفة

تم تحليل قاعدة البيانات ووجدنا المشاكل التالية:

### ❌ جداول مفقودة:
1. **`classes`** - الكود يبحث عنه لكنه غير موجود (يوجد `lessons` بدلاً منه)
2. **`trial_classes`** - مفقود تماماً

### ⚠️ أعمدة ناقصة:
- جدول `students` ينقصه: `phone`, `assigned_teacher`, `study_days`, `study_time`, `lesson_duration`, `monthly_payments`, `custom_notes`, `updated_at`
- جدول `teachers` ينقصه: `phone`, `country`, `hourly_rate`, `assigned_students`, `monthly_salary`, `updated_at`

---

## ✅ الحل الشامل

### الخطوة 1: تنفيذ SQL Script

1. اذهب إلى **Supabase Dashboard**: https://supabase.com/dashboard/project/qnviacuxtivlwblmtete
2. افتح **SQL Editor** من القائمة الجانبية
3. انسخ محتوى ملف `scripts/03-fix-database-schema.sql` بالكامل
4. الصق الكود في SQL Editor
5. اضغط **Run** (تشغيل)

### الخطوة 2: التحقق من النجاح

بعد تنفيذ السكريبت، تحقق من:

```sql
-- تحقق من وجود جدول classes
SELECT COUNT(*) FROM classes;

-- تحقق من وجود جدول trial_classes  
SELECT COUNT(*) FROM trial_classes;

-- تحقق من الأعمدة الجديدة في students
SELECT phone, assigned_teacher, study_days FROM students LIMIT 1;

-- تحقق من الأعمدة الجديدة في teachers
SELECT phone, country, hourly_rate FROM teachers LIMIT 1;
```

### الخطوة 3: اختبار النظام

1. ارجع إلى التطبيق: `http://localhost:3000`
2. سجّل الدخول باستخدام:
   - **Email**: `admin@alazhar.school`
   - **Password**: `mbanora1983`
3. تحقق من أن الـ Dashboard يعمل بدون أخطاء

---

## 📊 ما تم إصلاحه

### ✅ الجداول:
- إنشاء جدول `classes` (للحصص الدراسية)
- إنشاء جدول `trial_classes` (للحصص التجريبية)
- تحديث جداول `students` و `teachers` بالأعمدة المفقودة

### ✅ البيانات:
- 30 طالب (موجودين مسبقاً) + تحديث بياناتهم
- 5 معلمين (موجودين مسبقاً) + تحديث بياناتهم
- 10 حصص تجريبية جديدة
- 150 درس من جدول `lessons` (محفوظة)

### ✅ الفهارس (Indexes):
- إضافة indexes على جميع الأعمدة المهمة للأداء

---

## 🔐 إنشاء حساب Admin

إذا لم يكن لديك حساب Admin بعد:

1. اذهب إلى: https://supabase.com/dashboard/project/qnviacuxtivlwblmtete/auth/users
2. اضغط **Add user** → **Create new user**
3. أدخل:
   - **Email**: `admin@alazhar.school`
   - **Password**: `mbanora1983`
4. ✅ فعّل: **Auto Confirm User**
5. اضغط **Create user**

---

## 🚀 الخطوات النهائية

بعد تنفيذ كل ما سبق:

1. ✅ قاعدة البيانات جاهزة بالكامل
2. ✅ جميع الجداول موجودة ومتوافقة مع الكود
3. ✅ البيانات التجريبية جاهزة (30 طالب، 5 معلمين، 10 حصص تجريبية)
4. ✅ حساب Admin جاهز للدخول

**الآن يمكنك تسجيل الدخول والبدء في استخدام النظام! 🎉**

---

## ❓ في حالة وجود مشاكل

إذا واجهت أي خطأ:

1. تحقق من أن SQL script تم تنفيذه بالكامل بدون أخطاء
2. تحقق من أن حساب Admin موجود في Supabase Auth
3. تحقق من ملف `.env.local` يحتوي على:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://qnviacuxtivlwblmtete.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-anon-key>
   ```

4. أعد تشغيل السيرفر المحلي:
   ```bash
   npm run dev
